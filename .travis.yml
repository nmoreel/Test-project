language: c

services:
  - docker

stages:
  - build

jobs:
  include:
    - stage: build
    before_install:
      - docker pull epitechcontent/epitest-docker
    script: 
      - docker run -v `pwd`:/usr/local/bugs bash -c "cd /usr/local/bugs;make"
